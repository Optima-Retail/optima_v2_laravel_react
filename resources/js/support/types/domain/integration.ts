export type IntegrationListItem = {
    id: number;
    name: string;
    code: string;
    created_at: string | null;
};

export type IntegrationFormData = {
    id: number;
    name: string;
    code: string;
};
