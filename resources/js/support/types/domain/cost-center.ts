export type CostCenterListItem = {
    id: number;
    name: string;
    code: string;
    created_at: string | null;
};

export type CostCenterFormData = {
    id: number;
    name: string;
    code: string;
};
