export type CurrencyListItem = {
    id: number;
    name: string;
    code: string;
    created_at: string | null;
};

export type CurrencyFormData = {
    id: number;
    name: string;
    code: string;
};
