export type LanguageListItem = {
    id: number;
    name: string;
    code: string;
    created_at: string | null;
};

export type LanguageFormData = {
    id: number;
    name: string;
    code: string;
};
