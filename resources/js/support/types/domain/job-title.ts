export type JobTitleListItem = {
    id: number;
    name: string;
    code: string;
    created_at: string | null;
};

export type JobTitleFormData = {
    id: number;
    name: string;
    code: string;
};
