/** Shared shell chrome height so sidebar header and topbar stay pixel-aligned. */
export const SHELL_HEADER_CLASS =
    'flex h-12 min-h-12 max-h-12 shrink-0 items-center bg-surface ' +
    'shadow-[inset_0_-1px_0_0_var(--color-line)]';
